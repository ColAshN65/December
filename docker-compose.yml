version: '3.9'

services:
  python:
    image: ghcr.io/colashn65/december/python-app
    restart: always
    permissions:
      contents: read
      packages: write
  node:
    image: ghcr.io/colashn65/december/node-app
    restart: always
    permissions:
      contents: read
      packages: write
    ports:
      - 8080:8080
